# Databasens host (IP eller domän) – här körs databasen lokalt
DB_HOST=127.0.0.1

# Namn på databasen som Sequelize ska ansluta till
DB_NAME=wbshop_db

# Användarnamn för databasen
DB_USER=root

# Lösenord för databasanvändaren
DB_PASS=Admin

# Porten som databasen lyssnar på (för MySQL)
PORT=3305